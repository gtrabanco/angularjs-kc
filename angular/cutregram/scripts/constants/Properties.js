
//We'll define the app properties
//Constant because we'll need it in config phase
angular.module('cutregram').constant('Properties', {
    //apiKey: '', //I made it in different file in order to hide it from the repo
    ApiUrl: 'http://cutregram-sp.appspot.com/api/1'
});